import { createContext } from "react";
import { T_<%= upper_name %>State } from "../types/<%= upper_name %>State.type";

export const <%= name %>InitialState: T_<%= upper_name %>State = {};

export const <%= upper_name %>Context = createContext<T_<%= upper_name %>State>(<%= name %>InitialState);
