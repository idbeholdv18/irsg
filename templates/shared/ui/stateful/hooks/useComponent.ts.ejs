import { useContext } from "react";
import { <%= upper_name %>Context } from "../context/<%= upper_name %>.context";

export const use<%= upper_name %> = () => {
  return useContext(<%= upper_name %>Context);
};
