import { useContext } from "react";
import { Dispatch<%= upper_name %>Context } from "../context/Dispatch<%= upper_name %>.context";

export const use<%= upper_name %>Dispatch = () => {
  return useContext(Dispatch<%= upper_name %>Context);
};
